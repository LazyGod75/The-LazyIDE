/* Shared panel chrome for Settings > Account cards. */

import type { CSSProperties } from 'react';

export const ACCOUNT_PANEL_STYLE: CSSProperties = {
  padding: 16,
  background: 'var(--color-panel-2)',
  border: '1px solid var(--color-border)',
  borderRadius: 10,
};
